var React       = require('react');

/**
 * The navigation
 */
var Exp = React.createClass({
    render: function() {
        return (
            <div
                id={'exp-bar'}> 
            </div>
        );
    }
});

module.exports = Exp;
